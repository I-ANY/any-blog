+++
title = "基础包"
date = "2026-06-02T00:00:00+08:00"
draft = false
weight = 3
+++

# base64编码

使用案例：

```go
package main

import (
    "encoding/base64"
    "fmt"
)

func main() {
    test := `
	hello world
	go
	python
	java
	javascript
	`
    // 编码
    encodeStr := base64.StdEncoding.EncodeToString([]byte(test))
    fmt.Println(encodeStr)

    // 解码
    decodeByte, err := base64.StdEncoding.DecodeString(encodeStr)
    if err != nil {
        fmt.Println(err)
    }
    fmt.Printf("%s\n", string(decodeByte))
}

```

# cobra

## 介绍

参考：

-   Cobra 官网：[https://cobra.dev/](https://cobra.dev/)
-   Cobra 源码：[github.com/spf13/cobra](https://github.com/spf13/cobra)
-   Cobra 文档：[https://pkg.go.dev/github.com/spf13/cobra](https://pkg.go.dev/github.com/spf13/cobra)
-   Cobra-CLI 文档：[https://github.com/spf13/cobra-cli/blob/main/README.md](https://github.com/spf13/cobra-cli/blob/main/README.md)
-   掘金博客：[https://juejin.cn/post/7231197051203256379?searchId=202501191623199714EBC3BC6AE7FD3CD0#heading-0](https://juejin.cn/post/7231197051203256379?searchId=202501191623199714EBC3BC6AE7FD3CD0#heading-0)

  

Cobra 是一个 Go 语言开发的命令行（CLI）框架，它提供了简洁、灵活且强大的方式来创建命令行程序。它包含一个用于创建命令行程序的库（Cobra 库），以及一个用于快速生成基于 Cobra 库的命令行程序工具（Cobra 命令）。Cobra 是由 Go 团队成员 [spf13](https://link.juejin.cn?target=https%3A%2F%2Fspf13.com%2F) 为 [Hugo](https://link.juejin.cn?target=https%3A%2F%2Fgohugo.io%2F) 项目创建的，并已被许多流行的 Go 项目所采用，如 Kubernetes、Helm、Docker (distribution)、Etcd 等。

Cobra 建立在命令、参数和标志这三个结构之上。要使用 Cobra 编写一个命令行程序，需要明确这三个概念。

-   命令（COMMAND）：命令表示要执行的操作。
-   参数（ARG）：是命令的参数，一般用来表示操作的对象。
-   标志（FLAG）：是命令的修饰，可以调整操作的行为。

一个好的命令行程序在使用时读起来像句子，用户会自然的理解并知道如何使用该程序。

## 安装

```go
go get -u github.com/spf13/cobra/cobra
```

## 使用

### 简单使用

目录结构：

![image.png](assets/cobra-1.png)

```go
package cmd

import (
	"fmt"
	"github.com/spf13/cobra"
	"os"
)

var rootCmd = &cobra.Command{
	// 命令名称
	Use: "letgo",
	// 命令简单描述
	Short: "一个命令",
	// 详细描述
	Long: "这是一个非常牛逼的命令",
	// 命令执行方法， 当命令执行时， 会调用这个方法，args为参数列表
	Run: func(cmd *cobra.Command, args []string) {
		fmt.Printf("执行命令...")
	},
}

func Execute() {
	// 执行命令
	if err := rootCmd.Execute(); err != nil {
		fmt.Println(err)
		os.Exit(1)
	}
}
```

main函数

```go
package main

import "any.github.com/cmd"

func main() {
	cmd.Execute()
}
```

编译并运行命令

```bash
# 编译
[root@master1 go-demo]#  go build -o letgo
# 执行
[root@master1 go-demo]#  ./letgo
执行命令...

[root@master1 go-demo]# ./letgo -h
这是一个非常牛逼的命令

Usage:
  letgo [flags]

Flags:
  -h, --help   help for letgo

```

### 绑定参数

**持久标识**

该标志将可用于它所分配的命令以及该命令下的所有子命令w

**本地标识**

只适用于该指定命令

**结合使用：**

目录结构：

![image.png](assets/cobra-2.png)

```go
package cmd

import (
	"fmt"
	"github.com/spf13/cobra"
	"github.com/spf13/viper"
	"os"
)

var rootCmd = &cobra.Command{
	// 命令名称
	Use: "letgo",
	// 命令简单描述
	Short: "一个命令",
	// 详细描述
	Long: "这是一个非常牛逼的命令",
	// 命令执行方法， 当命令执行时， 会调用这个方法
	Run: func(cmd *cobra.Command, args []string) {
		fmt.Printf("执行test命令...\n")
		// 获取命令行参数，并打印
		fmt.Println("viper: ", cmd.Flags().Lookup("viper").Value)
		fmt.Println("author: ", cmd.PersistentFlags().Lookup("author").Value)
		fmt.Println("config: ", cmd.PersistentFlags().Lookup("config").Value)
		fmt.Println("license: ", cmd.PersistentFlags().Lookup("license").Value)
		fmt.Println("source: ", cmd.Flags().Lookup("source").Value)

		fmt.Println("------------config.yaml的值-------------------")
		fmt.Println(viper.GetString("author"))
		fmt.Println(viper.GetString("license"))
	},
}

func Execute() {
	// 执行命令
	if err := rootCmd.Execute(); err != nil {
		fmt.Println(err)
		os.Exit(1)
	}
}

var cfgFile string
var userLicense string

// 命令初始化
func init() {
	// 注册在命令初始化时需要执行的函数
	cobra.OnInitialize(initConfig)
	// 持久标志
	// 按名称接受命令行参数，不支持短标志，传参：--viper true|false
	rootCmd.PersistentFlags().Bool("viper", true, "是否使用viper")
	// 接受命令行参数，支持短标志，传参：--author zhangsan 或 -a zhangsan
	rootCmd.PersistentFlags().StringP("author", "a", "Any", "name")
	// 通过指针，将只复制到字段，不支持缩写
	rootCmd.PersistentFlags().StringVar(&cfgFile, "config", "", "配置文件")
	// 通过指针，将只复制到字段，并指定缩写
	rootCmd.PersistentFlags().StringVarP(&userLicense, "license", "l", "", "证书")
	// 添加本地标志
	rootCmd.Flags().StringP("source", "s", "", "source")

	// 从配置文件绑定到命令行标志
	_ = viper.BindPFlag("author", rootCmd.PersistentFlags().Lookup("author"))
	_ = viper.BindPFlag("license", rootCmd.PersistentFlags().Lookup("license"))
	// 配置viper默认值
	viper.SetDefault("author", "default author")
	viper.SetDefault("license", "default license")
}

// 初始化配置文件
func initConfig() {
	if cfgFile != "" {
		// 从指定路径读取配置
		viper.SetConfigFile(cfgFile)
	} else {
		// 从当前目录读取配置
		path, err := os.Getwd()
		// 检查给定的错误 err 是否为非空。如果 err 不为空，则会调用 os.Exit(1) 终止程序，并打印错误信息。
		cobra.CheckErr(err)
		viper.AddConfigPath(path)
		viper.SetConfigType("yaml")
		viper.SetConfigName("default-config")
	}
	// 检查并读取环境变量，将配置的键值加载到 viper 中，并检查错误。如果读取配置文件时出错，则会调用 os.Exit(1) 终止程序，并打印错误信息。
	viper.AutomaticEnv()
	if err := viper.ReadInConfig(); err != nil {
		cobra.CheckErr(err)
	}
	fmt.Println("Using config file:", viper.ConfigFileUsed())
}
```

执行：

查看帮助

```bash
# 编译执行
$ go build -o rootCmd
# 执行
$ ./rootCmd -h
这是一个非常牛逼的命令

Usage:
  root [flags]
  root [command]

Flags:
  -a, --author string    name (default "Any")
      --config string    配置文件
  -h, --help             help for root
  -l, --license string   证书
  -s, --source string    source
      --viper            是否使用viper (default true)

Use "root [command] --help" for more information about a command.

```
```go
// 直接run项目
PS E:\project\go-demo> go run .\main.go -h
这是一个非常牛逼的命令

Usage:
  root [flags]
  root [command]

Flags:
  -a, --author string    name (default "Any")
      --config string    配置文件
  -h, --help             help for root
  -l, --license string   证书
  -s, --source string    source
      --viper            是否使用viper (default true)

Use "root [command] --help" for more information about a command.

```

不带任何参数

```go
PS E:\project\go-demo> go run .\main.go
Using config file: E:\project\go-demo\default-config.yaml
执行root命令...
viper:  true
author:  Any
config:
license:
source:
------------config.yaml的值-------------------
default author
default license
```

带有参数，但是不指定配置文件，此时指定了，author与license ，只改变了viper中的值，并不改变config.yaml中的值

```go
PS E:\project\go-demo> go run .\main.go --viper=true -a Zhangsan -l license -s local
Using config file: E:\project\go-demo\default-config.yaml
执行root命令...
viper:  true
author:  Zhangsan
config:
license:  license
source:  local
------------config.yaml的值-------------------
Zhangsan
license
```

指定配置文件，此时

```go
PS E:\project\go-demo> go run .\main.go --viper=true --config=myconfig.yaml -s local  
Using config file: config.yaml
执行root命令...
viper:  true
author:  Any
config:  config.yaml
license:
source:  local
------------config.yaml的值-------------------
Any
MIT
```

### 添加子命令

修改`root.go`文件：

```go
var rootCmd = &cobra.Command{
    ...
    
    // 是否在执行目标命令之前解析每个命令的本地标志
	TraverseChildren: true,
}
```

添加`cmd/init.go`文件

```go
package cmd

import (
	"fmt"
	"github.com/spf13/cobra"
)

var add = cobra.Command{
	Use:   "add",
	Short: "add 命令",
	Long:  "这个是 add 命令",
	Run: func(cmd *cobra.Command, args []string) {
		fmt.Printf("执行add命令...\n")
		// 获取全局命令行参数，并打印
		fmt.Println("viper: ", cmd.Flags().Lookup("viper").Value)
		fmt.Println("author: ", cmd.Flags().Lookup("author").Value)
		fmt.Println("config: ", cmd.Flags().Lookup("config").Value)
		fmt.Println("license: ", cmd.Flags().Lookup("license").Value)
		// 获取父级命令行参数，并打印
		fmt.Println("source: ", cmd.Parent().Flags().Lookup("source").Value)
	},
}

// 添加子命令 add
func init() {
	rootCmd.AddCommand(&add)
}

```

执行结果：

查看帮助

```go
PS E:\project\go-demo> go run .\main.go -h
这是一个非常牛逼的命令

Usage:
  root [flags]
  root [command]

Available Commands:
  add         add 命令
  completion  Generate the autocompletion script for the specified shell
  help        Help about any command

Flags:
  -a, --author string    name (default "Any")
      --config string    配置文件
  -h, --help             help for root
  -l, --license string   证书
  -s, --source string    source
      --viper            是否使用viper (default true)

Use "root [command] --help" for more information about a command.

```

携带参数执行：

```go
PS E:\project\go-demo> go run .\main.go -s local add --viper=true -a Zhangsan -l license
Using config file: E:\project\go-demo\default-config.yaml
执行add命令...
viper:  true
author:  Zhangsan
config:
license:  license
source:  local
```

### 参数验证

在执行命令行程序时，我们可能需要对命令参数进行合法性验证，`cobra.Command` 的 `Args` 属性提供了此功能。

`Args` 属性类型为一个函数：`func(cmd *Command, args []string) error`，可以用来验证参数。

Cobra 内置了以下验证函数：

-   `NoArgs`：如果存在任何命令参数，该命令将报错。
-   `ArbitraryArgs`：该命令将接受任意参数。
-   `OnlyValidArgs`：如果有任何命令参数不在 `Command` 的 `ValidArgs` 字段中，该命令将报错。
-   `MinimumNArgs(int)`：如果没有至少 N 个命令参数，该命令将报错。
-   `MaximumNArgs(int)`：如果有超过 N 个命令参数，该命令将报错。
-   `ExactArgs(int)`：如果命令参数个数不为 N，该命令将报错。
-   `ExactValidArgs(int)`：如果命令参数个数不为 N，或者有任何命令参数不在 `Command` 的 `ValidArgs` 字段中，该命令将报错。
-   `RangeArgs(min, max)`：如果命令参数的数量不在预期的最小数量 `min` 和最大数量 `max` 之间，该命令将报错。

**使用内置验证参数：**

```go
var versionCmd = &cobra.Command{
	Use:   "version",
	Args: cobra.MaximumNArgs(2), 	// 使用内置的验证函数，位置参数多于 2 个则报错
    Run: func(cmd *cobra.Command, args []string) {
		fmt.Println("Hugo Static Site Generator v0.9 -- HEAD")
	},
}

```

**自定义参数验证**

```go
var printCmd = &cobra.Command{
	Use: "print [OPTIONS] [COMMANDS]",
	Run: func(cmd *cobra.Command, args []string) {
		fmt.Println("run print...")
		// 命令行位置参数列表：例如执行 `hugo print a b c d` 将得到 [a b c d]
		fmt.Printf("args: %v\n", args)
	},
	// 使用自定义验证函数
	Args: func(cmd *cobra.Command, args []string) error {
        // 长度小于1，报错
		if len(args) < 1 {
			return errors.New("requires at least one arg")
		}
        // 长度大于4，报错
		if len(args) > 4 {
			return errors.New("the number of args cannot exceed 4")
		}
        // 第一个参数不等于a，报错
		if args[0] != "a" {
			return errors.New("first argument must be 'a'")
		}
		return nil
	},
}
```

### Hooks

在执行 `Run` 函数前后，我么可以执行一些钩子函数，其作用和执行顺序如下：

1.  `PersistentPreRun`：在 `PreRun` 函数执行之前执行，对此命令的子命令同样生效。
2.  `PreRun`：在 `Run` 函数执行之前执行。
3.  `Run`：执行命令时调用的函数，用来编写命令的业务逻辑。
4.  `PostRun`：在 `Run` 函数执行之后执行。
5.  `PersistentPostRun`：在 `PostRun` 函数执行之后执行，对此命令的子命令同样生效。

修改`rootCmd.go`：

```go
var rootCmd = &cobra.Command{
	Use:   "letgo",
    // 在 PreRun 函数执行之前执行，对此命令的子命令同样生效
	PersistentPreRun: func(cmd *cobra.Command, args []string) {
		fmt.Println("letgo PersistentPreRun")
	},
    // run函数执行前执行
	PreRun: func(cmd *cobra.Command, args []string) {
		fmt.Println("letgo PreRun")
	},
    // 执行run函数
	Run: func(cmd *cobra.Command, args []string) {
		fmt.Println("run letgo...")
	},
    // 执行run函数后执行
	PostRun: func(cmd *cobra.Command, args []string) {
		fmt.Println("letgo PostRun")
	},
    // 在 PostRun 函数执行之后执行，对此命令的子命令同样生效
	PersistentPostRun: func(cmd *cobra.Command, args []string) {
		fmt.Println("letgo PersistentPostRun")
	},
}
```
```go
# 编译
$ go build -o letgo
# 执行
$ ./letgo
letgo PersistentPreRun
letgo PreRun
run letgo...
letgo PostRun
letgo PersistentPostRun
```

其中 `PersistentPreRun`、`PersistentPostRun` 两个函数对子命令同样生效。

```go
$ ./letgo version 
letgo PersistentPreRun
letgo Static Site Generator v0.9 -- HEAD
letgo PersistentPostRun
```

以上几个函数都有对应的 `<Hooks>E` 版本，`E` 表示 `Error`，即函数执行出错将会返回 `Error`，执行顺序

1.  `PersistentPreRunE`
2.  `PreRunE`
3.  `RunE`
4.  `PostRunE`
5.  `PersistentPostRunE`

注意：

1.  如果定义了 `<Hooks>E` 函数，则 `<Hooks>` 函数不会执行。比如同时定义了 `Run` 和 `RunE`，则只会执行 `RunE`，不会执行 `Run`，其他 `Hooks` 函数同理。
2.  如果子命令定义了自己的 `Persistent*Run` 函数，则不会继承父命令的 `Persistent*Run` 函数。

### 未知命令建议

在使用 `git` 命令时，有一个非常好用的功能，能够对用户输错的未知命令智能提示。

当输入一个不存在的命令 `statu` 时，`git` 会提示命令不存在，并且给出几个最相似命令的建议。

示例：

```go
$ git statu
git: 'statu' is not a git command. See 'git --help'.

The most similar commands are
        status
        stage
        stash
```

Cobra 自带了此功能。

```go
$ ./letgo vers      
Error: unknown command "vers" for "letgo"

Did you mean this?
        version

Run 'letgo --help' for usage.
unknown command "vers" for "letgo"

Did you mean this?
        version

```

注意：根据我的实测，要想让此功能生效，`Command.TraverseChildren` 属性要置为 `false`。

如果想彻底关闭此功能，可以使用如下设置：

```go
command.DisableSuggestions = true
```

或者使用如下设置调整字符串匹配的最小距离：

```go
command.SuggestionsMinimumDistance = 1
```

`SuggestionsMinimumDistance` 是一个正整数，表示输错的命令与正确的命令最多有几个不匹配的字符（最小距离），才会给出建议。如当值为 `1` 时，用户输入 `letgo versiox` 会给出建议，而如果用户输入 `letgo versixx` 时，则不会给出建议，因为已经有两个字母不匹配 `version` 了。

## 使用 Cobra 命令创建项目

Cobra 提供了 `cobra-cli` 命令行工具，可以通过命令的方式快速创建一个命令行项目。

安装：

```go
go install github.com/spf13/cobra-cli@latest
```

查看帮助：

```go
$ cobra-cli -h       
Cobra is a CLI library for Go that empowers applications.
This application is a tool to generate the needed files
to quickly create a Cobra application.

Usage:
  cobra-cli [command]

Available Commands:
  add         Add a command to a Cobra Application
  completion  Generate the autocompletion script for the specified shell
  help        Help about any command
  init        Initialize a Cobra Application

Flags:
  -a, --author string    author name for copyright attribution (default "YOUR NAME")
      --config string    config file (default is $HOME/.cobra.yaml)
  -h, --help             help for cobra-cli
  -l, --license string   name of license for the project
      --viper            use Viper for configuration

Use "cobra-cli [command] --help" for more information about a command.
```

### 初始化

要使用 `cobra-cli` 生成一个项目，首先要手动创建项目根目录并使用 `go mod` 命令进行初始化。

假设我们要编写的命令行程序叫作 `cog`，模块初始化过程如下：

```bash
# 创建并进入项目目录
mkdir cobra-demo && cd cobra-demo

# 初始化模块
go mod init github.com/any/cobra-demo

# 初始化命令行程序
cobra-cli init
Your Cobra application is ready at
E:\project\cobra-demo

# 查看目录结构
tree .
.
├── LICENSE
├── cmd
│   └── root.go
├── go.mod
├── go.sum
└── main.go

# 执行main程序
go run .\main.go
A longer description that spans multiple lines and likely contains
examples and usage of using your application. For example:

Cobra is a CLI library for Go that empowers applications.
This application is a tool to generate the needed files
to quickly create a Cobra application.

```

使用 `cobra-cli` 初始化程序非常方便，只需要一个简单的 `init` 命令即可完成。目录结构跟我们手动编写的程序相同，只不过多了一个 `LICENSE` 文件，用来存放项目的开源许可证。通过 `go run main.go` 执行这个命令行程序，即可打印 `rootCmd.Run` 的输出结果。

使用脚手架自动生成的 `cog/main.go` 文件内容如下：

```go
/*
Copyright © 2025 NAME HERE <EMAIL ADDRESS>

*/
package main

import "github.com/any/cobra-demo/cmd"

func main() {
    cmd.Execute()
}

```

自动生成的 `cog/cmd/root.go` 文件内容如下：

```go
/*
Copyright © 2025 NAME HERE <EMAIL ADDRESS>

*/
package cmd

import (
    "os"

    "github.com/spf13/cobra"
)

// rootCmd represents the base command when called without any subcommands
var rootCmd = &cobra.Command{
    Use:   "cobra-demo",
    Short: "A brief description of your application",
    Long: `A longer description that spans multiple lines and likely contains
examples and usage of using your application. For example:

Cobra is a CLI library for Go that empowers applications.
This application is a tool to generate the needed files
to quickly create a Cobra application.`,
    // Uncomment the following line if your bare application
    // has an action associated with it:
    // Run: func(cmd *cobra.Command, args []string) { },
}

// Execute adds all child commands to the root command and sets flags appropriately.
// This is called by main.main(). It only needs to happen once to the rootCmd.
func Execute() {
    err := rootCmd.Execute()
    if err != nil {
        os.Exit(1)
    }
}

func init() {
    // Here you will define your flags and configuration settings.
    // Cobra supports persistent flags, which, if defined here,
    // will be global for your application.

    // rootCmd.PersistentFlags().StringVar(&cfgFile, "config", "", "config file (default is $HOME/.cobra-demo.yaml)")

    // Cobra also supports local flags, which will only run
    // when this action is called directly.
    rootCmd.Flags().BoolP("toggle", "t", false, "Help message for toggle")
}
```

以上两个文件跟我们手动编写的代码没什么两样，套路完全相同，唯一不同的是每个文件头部都会多出来一个 `Copyright` 头信息，用来标记代码的 `LICENSE`。

### 可选标志

`cobra-cli` 提供了如下三个标志分别用来设置项目的作者、许可证类型、是否使用 Viper 管理配置。

指定可选标志后对项目进行了重新初始化。

```bash
cobra-cli init --author "Any" --license mit --viper
Your Cobra application is ready at
```

现在 `LICENSE` 文件内容不再为空，而是 `MIT` 协议。

```plain
The MIT License (MIT)

Copyright © 2025 Any

Permission is hereby granted...
```

且 Go 文件 `Copyright` 头信息中作者信息也会被补全。

```plain
/*
Copyright © 2025 Any

...
*/
```

`cobra-cli` 命令内置开源许可证支持 `GPLv2`、`GPLv3`、`LGPL`、`AGPL`、`MIT`、`2-Clause BSD` 或 `3-Clause BSD`。也可以参考[官方文档](https://link.juejin.cn/?target=https%3A%2F%2Fgithub.com%2Fspf13%2Fcobra-cli%2Fblob%2Fmain%2FREADME.md%23configuring-the-cobra-generator)来指定自定义许可证。

### 添加命令

使用 `add` 命令为程序添加新的命令，并且 `add` 命令同样支持可选标志。

```bash
# 添加rootCmd的子命令：server
cobra-cli add server
# 添加rootCmd的子命令：config
cobra-cli add config
# create 命令则通过 -p 'configCmd' 参数指定为 config 的子命令。
cobra-cli add create -p 'configCmd' --author "Any" --license mit --viper 
```

注意：使用 `-p 'configCmd'` 标志指定当前命令的父命令时，`configCmd` 必须是小驼峰命名法，因为 `cobra-cli` 为 `config` 生成的命令代码自动命名为 `configCmd`，而不是 `config_cmd` 或其他形式，这符合 Go 语言变量命名规范。

​  

现在命令行程序目录结构如下：

```bash
$ tree .
.
├── LICENSE
├── cmd
│   ├── config.go
│   ├── root.go
│   └── server.go
├── go.mod
├── go.sum
└── main.go
```

### 使用配置文件进行初始化

如果你不想每次生成或添加命令时都指定选项参数，则可以定义 `~/.cobra.yaml` 文件来保存配置信息：

```yaml
author: Any
year: 2025
license: MIT
useViper: true
```

再次使用initl初始化程序

```bash
cobra-cli init                                                                      
Using config file: /Users/jianghushinian/.cobra.yaml
```

会提示使用了 `~/.cobra.yaml` 配置文件。

如果不想把配置保存在 `~/.cobra.yaml` 中，`cobra-cli` 还提供了 `--config` 标志来指定任意目录下的配置文件。

至此，`cobra-cli` 的功能我们就都讲解完成了，还是非常方便实用的。

# flag

Go语言内置的`flag`包实现了命令行参数的解析，`flag`包使得开发命令行工具更为简单。

flag作为go的基础模块，属于必知必会的常用模块，flag实现命令行`flag`解析，比如`ls -l`中的`-l`​

### 导入flag包

```go
import flag
```

### flag参数类型

flag包支持的命令行参数类型有`bool`、`int`、`int64`、`uint`、`uint64`、`float` `float64`、`string`、`duration`。

| **flag参数** | **有效值** |
| --- | --- |
| 字符串flag | 合法字符串 |
| 整数flag | 1234、0664、0x1234等类型，也可以是负数。 |
| 浮点数flag | 合法浮点数 |
| bool类型flag | 1, 0, t, f, T, F, true, false, TRUE, FALSE, True, False。 |
| 时间段flag | 任何合法的时间段字符串。如”300ms”、”-1.5h”、”2h45m”。<br>合法的单位有”ns”、”us” /“µs”、”ms”、”s”、”m”、”h”。 |

### 定义命令行flag参数

有以下两种常用的定义命令行`flag`参数的方法。

#### flag.Type()

基本格式如下：

`flag.Type(flag名, 默认值, 帮助信息)*Type`

例如我们要定义姓名、年龄、婚否三个命令行参数，我们可以按如下方式定义：

```go
name := flag.String("name", "张三", "姓名")
age := flag.Int("age", 18, "年龄")
married := flag.Bool("married", false, "婚否")
delay := flag.Duration("d", 0, "时间间隔")
```

需要注意的是，此时`name`、`age`、`married`、`delay`均为对应类型的指针。

#### flag.TypeVar()

基本格式如下： `flag.TypeVar(Type指针, flag名, 默认值, 帮助信息)`

例如我们要定义姓名、年龄、婚否三个命令行参数，我们可以按如下方式定义：

```go
var name string
var age int
var married bool
var delay time.Duration

flag.StringVar(&name, "name", "张三", "姓名")
flag.IntVar(&age, "age", 18, "年龄")
flag.BoolVar(&married, "married", false, "婚否")
flag.DurationVar(&delay, "d", 0, "时间间隔")
```

### flag.Parse()

通过以上两种方法定义好命令行flag参数后，需要通过调用`flag.Parse()`来对命令行参数进行解析。

支持的命令行参数格式有以下几种：

-   `-flag xxx` （使用空格，一个`-`符号）
-   `--flag xxx` （使用空格，两个`-`符号）
-   `-flag=xxx` （使用等号，一个`-`符号）
-   `--flag=xxx` （使用等号，两个`-`符号）

其中，布尔类型的参数必须使用等号的方式指定。

Flag解析在第一个非flag参数（单个”-“不是flag参数）之前停止，或者在终止符”–“之后停止。

### flag其他函数

```go
flag.Args()  ////返回命令行参数后的其他参数，以[]string类型
flag.NArg()  //返回命令行参数后的其他参数个数
flag.NFlag() //返回使用的命令行参数个数
```

### 子命令的使用

```go
package main

import (
	"flag"
	"fmt"
)

func main(){
	var name string
	flag.Parse()
	goCmd := flag.NewFlagSet("go",flag.ExitOnError)
	goCmd.StringVar(&name,"name","Go语言","帮助信息")
	phpCmd := flag.NewFlagSet("php",flag.ExitOnError)
	phpCmd.StringVar(&name,"n","php语言","帮助信息")

	args := flag.Args()
	if len(args) <= 0{
		return
	}

	switch args[0] {
	case "go":
		_ = goCmd.Parse(args[1:])
		fmt.Println("go......")
	case "php":
		_ = phpCmd.Parse(args[1:])
		fmt.Println("php......")
	}
	fmt.Println(name)
}
```

由于需要处理子命令，因此调用了flag.NewFlagSet方法。该方法会返回带有指定名称和错误处理属性的空命令集，相当于创建一个新的命令集去支持子命令。需要注意的是，flag.NewFlagSet方法的第二个参数是ErrorHandling，用于指定处理异常错误，其内置了以下三种模式：

-   ContinueOnError
-   ExitOnError
-   PanicOnError

### 定义参数类型

```go
package main

import (
	"errors"
	"flag"
	"fmt"
)

type Name string
func (n *Name)String() string{
	return fmt.Sprint(*n)
}

func (n *Name) Set(value string) error  {
	if len(*n) > 0{
		return errors.New("value is already set")
	}

	*n = Name("joker:"+value)
	return nil
}

func main(){
	var name Name
	flag.Var(&name,"name","帮助信息")
	flag.Parse()

	fmt.Printf("name: %s",name)
}
```

我们只需要实现Value的两个接口，如下：

```go
type Value interface {
	String() string
	Set(string) error
}
```

然后就实现了自定义参数类型了。

### 完整示例

#### 定义

```go
func main() {
	//定义命令行参数方式1
	var name string
	var age int
	var married bool
	var delay time.Duration
    
	flag.StringVar(&name, "name", "张三", "姓名")
	flag.IntVar(&age, "age", 18, "年龄")
	flag.BoolVar(&married, "married", false, "婚否")
	flag.DurationVar(&delay, "d", 0, "延迟的时间间隔")
    
	//解析命令行参数
	flag.Parse()
	fmt.Println(name, age, married, delay)
	//返回命令行参数后的其他参数
	fmt.Println(flag.Args())
	//返回命令行参数后的其他参数个数
	fmt.Println(flag.NArg())
	//返回使用的命令行参数个数
	fmt.Println(flag.NFlag())
}
```

#### 使用

命令行参数使用提示：

```go
$ ./flag_demo -help
Usage of ./flag_demo:
  -age int
        年龄 (default 18)
  -d duration
        时间间隔
  -married
        婚否
  -name string
        姓名 (default "张三")
```

正常使用命令行flag参数：

```go
$ ./flag_demo -name 沙河娜扎 --age 28 -married=false -d=1h30m
沙河娜扎 28 false 1h30m0s
[]
0
4
```

使用非flag命令行参数：

```go
$ ./flag_demo a b c
张三 18 false 0s
[a b c]
3
0
```

# iota

一个特殊常量标识符，用于在常量声明（`const`）中创建一系列相关值，它会自动递增。

### 基本特性

1.  自动递增：`iota`从0开始，在每个const声明块中自动递增（每行+1）
2.  重置规则：每个新的`const`声明块都会重置`iota`为0
3.  表达式参与：可以与表达式结合使用

## 使用

### 基本

```go
const (
    A = iota  // 0
    B         // 1 (自动继承上面的表达式)
    C         // 2
)
```

### 自定义起始值

```go
const (
    A = iota + 1  // 1 (0+1)
    B             // 2 (1+1)
    C             // 3 (2+1)
)
```

### 跳过值

```go
const (
    A = iota  // 0
    _         // 1 (被跳过)
    B         // 2
    C         // 3
)
```

### 位掩码/标志位

```go
const (
    Read = 1 << iota  // 1 << 0 = 1
    Write             // 1 << 1 = 2
    Execute           // 1 << 2 = 4
)
```

位运算

数字 `1` 的二进制表示为：`00000000 00000000 00000000 00000001` （32位系统）

`x << n`（将 `x` 的二进制表示向左移动 `n` 位，右侧补0）

```go
1 (十进制) = 00000000 00000000 00000000 00000001 (二进制)

1 << 100
原始:   00000000 00000000 00000000 00000001
左移10位: 00000000 00000000 00000100 00000000

从右往左，第10位是1（位数从0开始计数），所以值为：
2^10 = 1024
```

### 结合表达式

```go
const (
    KB = 1 << (10 * iota)  // 1 << (10*0) = 1
    MB                     // 1 << (10*1) = 1024
    GB                     // 1 << (10*2) = 1048576
)
```

### 多常量声明

```go
const (
    A, B = iota, iota + 1  // A=0, B=1
    C, D                   // C=1, D=2
)
```

# json

## 序列化

将数据结构或对象序列化为json字符串

go中使用json包的`json.Marshal()`方法

```go
package main

import (
	"encoding/json"
	"fmt"
)

func main() {
	type address struct {
		City     string `json:"city"`
		Province string `json:"province"`
	}

	var a = address{
		City:     "深圳",
		Province: "广东",
	}
	aJson, err := json.Marshal(a)
	if err != nil {
		fmt.Printf("json.Marshal failed: %v", err)
	}
	fmt.Println(string(aJson))
	fmt.Println(a.Province, a.City)
}
```

输出：

```go
{"city":"广东","province":"深圳"}
广东 深圳
```

## 反序列化

将JSON字符串转换为数据结构或对象

go中使用json包的json.UnMarshal()方法

```go
package main

import (
	"encoding/json"
	"fmt"
)

func main() {
	type address struct {
		City     string `json:"city"`
		Province string `json:"province"`
	}

	jsonStr := `{"city":"广东","province":"深圳"}`
	var b address
	err := json.Unmarshal([]byte(jsonStr), &b)
	if err != nil {
		fmt.Printf("json.Unmarshal failed: %v", err)
	}
	fmt.Printf("%T\n", b)
	fmt.Printf("%+v\n", b)
}
```

输出：

```go
main.address
{City:深圳 Province:广东}
广东 深圳 
```

# logger

### 使用Logger

log包定义了Logger类型，该类型提供了一些格式化输出的方法。本包也提供了一个预定义的“标准”logger，可以通过调用函数`Print系列`(Print|Printf|Println）、`Fatal系列`（Fatal|Fatalf|Fatalln）、和`Panic系列`（Panic|Panicf|Panicln）来使用，比自行创建一个logger对象更容易使用。

例如，我们可以像下面的代码一样直接通过`log`包来调用上面提到的方法，默认它们会将日志信息打印到终端界面：

```go
package main
import (
    "log"
)
func main() {
    log.Println("这就是一条很普通的日志。")
    v := "有点特殊"
    log.Printf("这是一条%s日志。\n", v)
    log.Fatalln("这是一条会触发fatal的日志。")
    log.Panicln("这是一条会触发panic的日志。")
}
```

编译并执行上面的代码会得到如下输出：

```go
2024/10/31 13:41:55 这是一条很普通的日志。
2024/10/31 13:41:55 这是一条有点特殊的日志。
2024/10/31 13:41:55 这是一条会触发fatal的日志。
```

logger会打印每条日志信息的日期、时间，默认输出到系统的标准错误。Fatal系列函数会在写入日志信息后调用os.Exit(1)。Panic系列函数会在写入日志信息后panic。

### 配置logger

#### 标准logger的配置

默认情况下的logger只会提供日志的时间信息，但是很多情况下我们希望得到更多信息，比如记录该日志的文件名和行号等。`log`标准库中为我们提供了定制这些设置的方法。

`log`标准库中的`Flags`函数会返回标准logger的输出配置，而`SetFlags`函数用来设置标准logger的输出配置。

```go
func Flags() int
func SetFlags(flag int)
```

#### flag选项

`log`标准库提供了如下的flag选项，它们是一系列定义好的常量。

```go
const (
    // 控制输出日志信息的细节，不能控制输出的顺序和格式。
    // 输出的日志在每一项后会有一个冒号分隔：例如2009/01/23 01:23:23.123123 /a/b/c/d.go:23: message
    Ldate         = 1 << iota     // 日期：2009/01/23
    Ltime                         // 时间：01:23:23
    Lmicroseconds                 // 微秒级别的时间：01:23:23.123123（用于增强Ltime位）
    Llongfile                     // 文件全路径名+行号： /a/b/c/d.go:23
    Lshortfile                    // 文件名+行号：d.go:23（会覆盖掉Llongfile）
    LUTC                          // 使用UTC时间
    LstdFlags     = Ldate | Ltime // 标准logger的初始值
)
```

下面我们在记录日志之前先设置一下标准logger的输出选项如下：

```go
func main() {
    log.SetFlags(log.Llongfile | log.Lmicroseconds | log.Ldate)
    log.Println("这是一条很普通的日志。")
}
```

编译执行后得到的输出结果如下：

```go
2020/03/31 19:12:18.708939 E:/DEV/Go/src/code.rookieops.com/my_test/log_test/main.go:7: 这是一条很普通的日志。
```

#### 配置日志前缀

`log`标准库中还提供了关于日志信息前缀的两个方法：

```go
func Prefix() string
func SetPrefix(prefix string)
```

其中`Prefix`函数用来查看标准logger的输出前缀，`SetPrefix`函数用来设置输出前缀。

```go
func main() {
    log.SetFlags(log.Llongfile | log.Lmicroseconds | log.Ldate)
    log.Println("这是一条很普通的日志。")
    log.SetPrefix("[小王子]")
    log.Println("这是一条很普通的日志。")
}
```

上面的代码输出如下：

```go
2020/03/31 19:12:54.569050 E:/DEV/Go/src/code.rookieops.com/my_test/log_test/main.go:7: 这是一条很普通的日志。
[小王子]2020/03/31 19:12:54.631881 E:/DEV/Go/src/code.rookieops.com/my_test/log_test/main.go:9: 这是一条很普通的日志。
```

这样我们就能够在代码中为我们的日志信息添加指定的前缀，方便之后对日志信息进行检索和处理。

#### 配置日志输出位置

```go
func SetOutput(w io.Writer)
```

`SetOutput`函数用来设置标准logger的输出目的地，默认是标准错误输出。

例如，下面的代码会把日志输出到同目录下的`xx.log`文件中。

```go
func main() {
    logFile, err := os.OpenFile("./xx.log", os.O_CREATE|os.O_WRONLY|os.O_APPEND, 0644)
    if err != nil {
        fmt.Println("open log file failed, err:", err)
        return
    }
    log.SetOutput(logFile)
    log.SetFlags(log.Llongfile | log.Lmicroseconds | log.Ldate)
    log.Println("这是一条很普通的日志。")
    log.SetPrefix("[小王子]")
    log.Println("这是一条很普通的日志。")
}
```

如果你要使用标准的logger，我们通常会把上面的配置操作写到`init`函数中。

```go
func init() {
    logFile, err := os.OpenFile("./xx.log", os.O_CREATE|os.O_WRONLY|os.O_APPEND, 0644)
    if err != nil {
        fmt.Println("open log file failed, err:", err)
        return
    }
    log.SetOutput(logFile)
    log.SetFlags(log.Llongfile | log.Lmicroseconds | log.Ldate)
}
```

### 创建logger

`log`标准库中还提供了一个创建新logger对象的构造函数–`New`，支持我们创建自己的logger示例。`New`函数的签名如下：

```go
func New(out io.Writer, prefix string, flag int) *Logger
```

New创建一个Logger对象。其中，参数out设置日志信息写入的目的地。参数prefix会添加到生成的每一条日志前面。参数flag定义日志的属性（时间、文件等等）。

举个例子：

```go
func main() {
    logger := log.New(os.Stdout, "<New>", log.Lshortfile|log.Ldate|log.Ltime)
    logger.Println("这是自定义的logger记录的日志。")
}
```

将上面的代码编译执行之后，得到结果如下：

```go
<New>2020/03/31 19:13:42 main.go:8: 这是自定义的logger记录的日志。
```

## 参考

[https://www.yuque.com/coolops/golang/hfz054](https://www.yuque.com/coolops/golang/hfz054)

# strconv

strconv包实现了基本数据类型与其字符串表示的转换，主要有以下常用函数： `Atoi()`、`Itia()`、parse系列、format系列、append系列。

更多函数请查看[官方文档](https://golang.org/pkg/strconv/)。

### string与int类型转换

这一组函数是我们平时编程中用的最多的。

#### Atoi()

`Atoi()`函数用于将字符串类型的整数转换为int类型，函数签名如下。

```go
func Atoi(s string) (i int, err error)
```

如果传入的字符串参数无法转换为int类型，就会返回错误。

```go
s1 := "100"
i1, err := strconv.Atoi(s1)
if err != nil {
    fmt.Println("can't convert to int")
} else {
    fmt.Printf("type:%T value:%#v\n", i1, i1) //type:int value:100
}
```

#### Itoa()

`Itoa()`函数用于将int类型数据转换为对应的字符串表示，具体的函数签名如下。

```go
func Itoa(i int) string
```

示例代码如下：

```go
i := 200
s := strconv.Itoa(i)
fmt.Printf("type:%T value:%#v\n", s, s) //type:string value:"200"
```

### Parse系列函数

Parse类函数用于转换字符串为给定类型的值：ParseBool()、ParseFloat()、ParseInt()、ParseUint()。

#### ParseBool()

将字符串解析为布尔值。

```go
func ParseBool(str string) (value bool, err error)
```

返回字符串表示的bool值。它接受的字符串有：`1、0、t、f、T、F、true、false、True、False、TRUE、FALSE`；否则返回错误。

```go
str1 := "true"
boolValue, err := strconv.ParseBool(str1)
if err != nil {
    fmt.Println("Error:", err)
} else {
    fmt.Println("Boolean value:", boolValue)   //输出：Boolean value: true
}
```

#### ParseInt()

用于将字符串解析为整数。它需要三个参数：字符串、基数和位大小。

```go
func ParseInt(s string, base int, bitSize int) (i int64, err error)
```

返回字符串表示的整数值，接受正负号。

base指定进制（2到36），如果base为0，则会从字符串前置判断，”0x”是16进制，”0”是8进制，否则是10进制；

bitSize指定结果必须能无溢出赋值的整数类型，0、8、16、32、64 分别代表 int、int8、int16、int32、int64；

返回的err是\*NumErr类型的，如果语法有误，err.Error = ErrSyntax；如果结果超出类型范围err.Error = ErrRange。

```go
package main

import (
	"fmt"
	"strconv"
)

func main() {
	str := "12345"
	base := 10 // 进制，这里使用十进制
	bitSize := 64 // 整数位数，这里使用64位
	num, err := strconv.ParseInt(str, base, bitSize)
	if err != nil {
		fmt.Println("Error parsing integer:", err)
		return
	}
	fmt.Printf("Parsed integer: %d (type: %T)\n", num, num)   // 输出：Parsed integer: 12345 (type: int64)
}
```

#### ParseUnit()

功能类似于 `ParseInt`，但它不接受负号，用于解析无符号整数。

```go
func ParseUint(s string, base int, bitSize int) (n uint64, err error)
```

`ParseUint`类似`ParseInt`但不接受正负号，用于无符号整型。

```go
package main

import (
	"fmt"
	"strconv"
)

func main() {
	str := "123"
	uintValue, err := strconv.ParseUint(str, 10, 64)
	if err != nil {
		fmt.Println("Error:", err)
	} else {
		fmt.Println("Unsigned integer value:", uintValue) // 输出: Unsigned integer value: 123
	}
}
```

#### ParseFloat()

将字符串解析为浮点数并返回其值。

```go
func ParseFloat(s string, bitSize int) (f float64, err error)
```

如果s合乎语法规则，函数会返回最为接近s表示值的一个浮点数（使用IEEE754规范舍入）。

bitSize指定了期望的接收类型，32是float32（返回值可以不改变精确值的赋值给float32），64是float64；

返回值err是`*NumErr`类型的，语法有误的，`err.Error=ErrSyntax`；结果超出表示范围的，返回值f为`±Inf，err.Error= ErrRange`。

```go
package main

import (
	"fmt"
	"strconv"
)

func main() {
	str := "3.14159"
	bitSize := 64 // 浮点数位数，这里使用64位

	num, err := strconv.ParseFloat(str, bitSize)
	if err != nil {
		fmt.Println("Error parsing float:", err)
		return
	}
	fmt.Printf("Parsed float: %.5f (type: %T)\n", num, num)   // 输出：Parsed float: 3.14159 (type: float64)
}
```

### Format系列函数

Format系列函数实现了将给定类型数据格式化为`string`类型数据的功能。

#### FormatBool()

```go
func FormatBool(b bool) string
```

根据b的值返回”true”或”false”。

```go
package main

import (
	"fmt"
	"strconv"
)

func main() {
	strTrue := "true"
	strFalse := "false"

	boolTrue, err := strconv.ParseBool(strTrue)
	if err != nil {
		fmt.Println("Error parsing boolean (true):", err)
		return
	}

	boolFalse, err := strconv.ParseBool(strFalse)
	if err != nil {
		fmt.Println("Error parsing boolean (false):", err)
		return
	}

	fmt.Printf("Parsed boolean (true): %t (type: %T)\n", boolTrue, boolTrue)   // 输出：Parsed boolean (true): true (type: bool)
	fmt.Printf("Parsed boolean (false): %t (type: %T)\n", boolFalse, boolFalse) // 输出：Parsed boolean (false): false (type: bool)
}
```

#### FormatInt()

将整数格式化为字符串。

```go
func FormatInt(i int64, base int) string
```

返回i的base进制的字符串表示。base 必须在2到36之间，结果中会使用小写字母’a’到’z’表示大于10的数字。

```go
package main
 
import (
	"fmt"
	"strconv"
)
 
func main() {
	num := uint64(67890)
	str := strconv.FormatUint(num, 10) // 10表示十进制
 
	fmt.Printf("Formatted unsigned integer: %s (type: %T)\n", str, str)
}
```

#### FormatUint()

将无符号整数格式化为字符串。

```go
func FormatUint(i uint64, base int) string
```
```go
package main

import (
	"fmt"
	"strconv"
)

func main() {
	num := uint64(67890)
	str := strconv.FormatUint(num, 10) // 10表示十进制

	fmt.Printf("Formatted unsigned integer: %s (type: %T)\n", str, str)
}
```

#### FormatFloat()

将浮点数格式化为字符串。

```go
func FormatFloat(f float64, fmt byte, prec, bitSize int) string
```

bitSize表示f的来源类型（32：float32、64：float64），会据此进行舍入。

fmt表示格式：’f’（-ddd.dddd）、’b’（-ddddp±ddd，指数为二进制）、’e’（-d.dddde±dd，十进制指数）、’E’（-d.ddddE±dd，十进制指数）、’g’（指数很大时用’e’格式，否则’f’格式）、’G’（指数很大时用’E’格式，否则’f’格式）。

prec控制精度（排除指数部分）：对’f’、’e’、’E’，它表示小数点后的数字个数；对’g’、’G’，它控制总的数字个数。如果prec 为-1，则代表使用最少数量的、但又必需的数字来表示f。

```go
package main

import (
	"fmt"
	"strconv"
)

func main() {
	num := 3.14159
	str := strconv.FormatFloat(num, 'f', 2, 64) // 'f'表示小数形式，2表示小数点后保留两位，64表示位大小（对于float64）

	fmt.Printf("Formatted float: %s (type: %T)\n", str, str)
}
```

### 其他

#### isPrint()

```go
func IsPrint(r rune) bool
```

返回一个字符是否是可打印的，和`unicode.IsPrint`一样，r必须是：字母（广义）、数字、标点、符号、ASCII空格。

#### CanBackquote()

```go
func CanBackquote(s string) bool
```

返回字符串s是否可以不被修改的表示为一个单行的、没有空格和tab之外控制字符的反引号字符串。

#### 其他

除上文列出的函数外，`strconv`包中还有Append系列、Quote系列等函数。具体用法可查看[官方文档](https://golang.org/pkg/strconv/)。

## 参考

[https://www.yuque.com/coolops/golang/ofht37](https://www.yuque.com/coolops/golang/ofht37)

# template

template是Go语言的文本模板引擎，它提供了两个标准库。这两个标准库使用了同样的接口，但功能略有不同，具体如下：

-   text/template：基于模板输出文本内容
-   html/template：基于模板输出安全的HTML格式的内容，可以理解为其进行了转义，以避免受某些注入攻击

**使用**`**text/template**`**简单示例：**

```go
package main

import (
	"html/template"
	"os"
	"strings"
)

const templateText = `
output 0: {{title .Name1}}
output 1: {{title .Name2}}
output 2: {{.Name3 | title}}
`

func main()  {
	funcMap := template.FuncMap{"title": strings.Title}
	tp1 := template.New("go-test")
	tp1,_=tp1.Funcs(funcMap).Parse(templateText)
	data := map[string]string{
		"Name1": "go",
		"Name2": "php",
		"Name3": "python",
	}
	_ = tp1.Execute(os.Stdout, data)
}
```

首先调用标准库text/template中的New方法，其根据我们给定的名称标识创建了一个全新的模板对象。接下来调用Parse方法，将常量templateText（预定义的待解析模板）解析为当前文本模板的主体内容。最后调用Execute 方法，进行模板渲染。

简单来说，就是将传入的data动态参数渲染到对应的模板标识位上。因为我们将Execute方法的io.Writer指定到了os.Stdout中。

**使用**`**html/template**`**示例1：**

```go
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Go Web</title>
</head>
<body>
   <p>加油!  {{ . }}  同学！</p>
</body>
</html>
```
```go
package main

import (
        "fmt"
        "html/template"
        "net/http"
)

func process(w http.ResponseWriter, r *http.Request) {
        //  解析模板
        t, err := template.ParseFiles("./hello.tmpl")
        /**
        // 等价于如下代码部分
		t := template.New("hello.html")
		hello := `<!DOCTYPE html>
		<html lang="en">
		  <head>
    		<meta charset="UTF-8">
    		<meta name="viewport" content="width=device-width, initial-scale=1.0">
    	    <title>Go Web</title>
		  </head>
		    <body>
   			  <p>加油!  {{ . }}同学！</p>
		    </body>
		</html>`
		t, _ := t.Parse(hello)
		*/
        if err != nil {
                fmt.Println("Parsing the template file failed: %v", err)
                return
        }
        // 渲染模板
        t.Execute(w, "ANY")
}

func main() {
        http.HandleFunc("/", process)
        err := http.ListenAndServe(":8080", nil)
        if err != nil {
                fmt.Println("Running HTTP server is failed: %v", err)
                return
        }
}
```

执行程序后访问指定网址 [http://127.0.0.1:8080](http://127.0.0.1:8080/)，浏览到页面内容如下：

![image.png](assets/template-1.png)

**示例2：****传入一个结构体对象**

当前目录下准备一个文件：

```go
<html>
<header>
    <title>
      {{.Title}}
    </title>
</header>
<body>
    <h1>This is {{.Name}}</h1>
    <h2>My age is {{.Age}}</h2>
</body>
</html>
```
```go
package main

import (
	"html/template"
	"os"
)

type Persion struct {
	Title string
	Name  string
	Age   int
}

func main() {
	p := Persion{
		Title: "MY WORLD!",
		Name:  "Any",
		Age:   30,
	}
	// 创建一个空文件
	file, err := os.Create("tmp/any.html")
	if err != nil {
		panic(err)
	}
	defer file.Close()

	// 读取模板文件
	t, err := template.ParseFiles("tmp/any.tpl")
	if err != nil {
		panic(err)
	}
	err = t.Execute(file, p)      // 输出到文件
	err = t.Execute(os.Stdout, p) // 直接输出
}
```

输出：

```go
<html>
<header>
    <title>
      MY WORLD!
    </title>
</header>
<body>
    <h1>This is Any</h1>
    <h2>My age is 30</h2>
</body>
</html>
```

**template模板定义：**

-   双层大括号：也就是`{{}}`标识符，在template中，所有的动作（Actions）、数据评估（Data Evaluations）、控制流转都需要用标识符双层大括号包裹，其余的模板内容均全部原样输出
-   点（DOT）：会根据点（DOT）标识符进行模板变量的渲染，其参数可以为任何值，但特殊的复杂类型需进行特殊处理。例如，当为指针时，内部会在必要时自动表示为指针所指向的值。如果执行结果生成了一个函数类型的值，如结构体的函数类型字段，那么该函数不会自动调用
-   函数调用：在前面的代码中，通过FuncMap方法注册了名title的自定义函数。在模板渲染中一共用了两类处理方法，即使用{{title.Name1}}和管道符（|）对.Name3 进行处理。在template中，会把管道符前面的运算结果作为参数传递给管道符后面的函数，最终，命令的输出结果就是这个管道的运算结果

# time

time包提供了时间的显示和测量用的函数。日历的计算采用的是公历。

```go
2006-01-02 15:04:05.000
```

## 事件类型: time.Time

`time.Time`类型表示时间。

## 当前时间: time.Now()

`time.Now()`函数获取当前的时间对象，然后获取时间对象的年月日时分秒等信息。示例代码如下：

```go
func timeDemo() {
    now := time.Now() //获取当前时间：
    fmt.Printf("current time:%v\n", now)   // Current time: 2024-11-27 12:00:00.000000 +0800 CST
    year := now.Year()     //年
    month := now.Month()   //月
    day := now.Day()       //日
    hour := now.Hour()     //小时
    minute := now.Minute() //分钟
    second := now.Second() //秒
    fmt.Printf("%d-%02d-%02d %02d:%02d:%02d\n", year, month, day, hour, minute, second)
}
```

## 时间戳: now.Unix()

时间戳是自1970年1月1日（08:00:00GMT）至当前时间的总毫秒数。它也被称为Unix时间戳（UnixTimestamp）。

基于时间对象获取时间戳的示例代码如下：

```go
func timestampDemo() {
    now := time.Now()            //获取当前时间
    timestamp1 := now.Unix()     //时间戳
    timestamp2 := now.UnixNano() //纳秒时间戳
    fmt.Printf("current timestamp1:%v\n", timestamp1)
    fmt.Printf("current timestamp2:%v\n", timestamp2)
}
```

使用`time.Unix()`函数可以将时间戳转为时间格式。

```go
func timestampDemo2(timestamp int64) {
    timeObj := time.Unix(timestamp, 0) //将时间戳转为时间格式
    fmt.Println(timeObj)
    year := timeObj.Year()     //年
    month := timeObj.Month()   //月
    day := timeObj.Day()       //日
    hour := timeObj.Hour()     //小时
    minute := timeObj.Minute() //分钟
    second := timeObj.Second() //秒
    fmt.Printf("%d-%02d-%02d %02d:%02d:%02d\n", year, month, day, hour, minute, second)
}
```

## 时间间隔: time.Since()

`time.Duration`是`time`包定义的一个类型，它代表两个时间点之间经过的时间，以纳秒为单位。

`time.Duration`表示1纳秒，，可表示的最长时间段大约290年。

`time.Second`表示1秒。

time包中定义的时间间隔类型的常量如下：

```go
const (
    Nanosecond  Duration = 1
    Microsecond          = 1000 * Nanosecond
    Millisecond          = 1000 * Microsecond
    Second               = 1000 * Millisecond
    Minute               = 60 * Second
    Hour                 = 60 * Minute
)
```

​  

计算两个时间点之间的差异。

```go
package main

import (
	"fmt"
	"time"
)

func main() {
	then := time.Now()
	time.Sleep(2 * time.Second)
	duration := time.Since(then)
	fmt.Println("Time passed:", duration)   // 输出：Time passed: 2.01s
}
```

## time.Until()

计算直到某个特定时间点的剩余时间。

```go
package main

import (
	"fmt"
	"time"
)

func main() {
	targetTime := time.Date(2025, 1, 1, 0, 0, 0, 0, time.UTC)
	duration := time.Until(targetTime)
	fmt.Println("Time until New Year 2025:", duration)  //输出：Time until New Year 2025: 60 days
}
```

## time.Sleep()

暂停当前goroutine的执行指定的时间。

```go
package main

import (
	"fmt"
	"time"
)

func main() {
	fmt.Println("Sleeping for 2 seconds...")
	time.Sleep(2 * time.Second)
	fmt.Println("Done sleeping.")
}
```

## 时间操作

### Add

我们在日常的编码过程中可能会遇到要求时间+时间间隔的需求，Go语言的时间对象有提供Add方法如下：

```go
func (t Time) Add(d Duration) Time
```

举个例子，求一个小时之后的时间：

```go
func main() {
    now := time.Now()
    later := now.Add(time.Hour) // 当前时间加1小时后的时间
    fmt.Println(later)
}
```

### Sub

求两个时间之间的差值：

```go
func (t Time) Sub(u Time) Duration
```

返回一个时间段t-u。如果结果超出了Duration可以表示的最大值/最小值，将返回最大值/最小值。要获取时间点t-d（d为Duration），可以使用t.Add(-d)。

```go
package main

import (
	"fmt"
	"time"
)

func main() {
	// 定义两个时间点
	startTime := time.Date(2024, 10, 1, 12, 0, 0, 0, time.UTC)
	endTime := time.Date(2024, 10, 2, 12, 0, 0, 0, time.UTC)

	// 计算时间差
	duration := endTime.Sub(startTime)

	// 输出结果
	fmt.Printf("The duration between startTime and endTime is: %v\n", duration) // 输出：24h0m0s
}
```

### Equal

```go
func (t Time) Equal(u Time) bool
```

判断两个时间是否相同，会考虑时区的影响，因此不同时区标准的时间也可以正确比较。本方法和用t==u不同，这种方法还会比较地点和时区信息。

```go
package main

import (
	"fmt"
	"time"
)

func main() {
	// 定义两个时间点
	time1 := time.Date(2023, 10, 1, 12, 0, 0, 0, time.UTC)
	time2 := time.Date(2023, 10, 1, 12, 0, 0, 0, time.UTC)
	time3 := time.Date(2023, 10, 1, 12, 0, 1, 0, time.UTC)

	// 判断时间是否相等
	fmt.Println(time1.Equal(time2)) // 输出：true
	fmt.Println(time1.Equal(time3)) // 输出：false
}
```

### Before

```go
func (t Time) Before(u Time) bool
```

如果t代表的时间点在u之前，返回真；否则返回假。

```go
package main

import (
	"fmt"
	"time"
)

func main() {
	// 定义两个时间点
	time1 := time.Date(2023, 10, 1, 12, 0, 0, 0, time.UTC)
	time2 := time.Date(2023, 10, 2, 12, 0, 0, 0, time.UTC)

	// 判断时间先后
	fmt.Println(time1.Before(time2)) // 输出：true
	fmt.Println(time2.Before(time1)) // 输出：false
}
```

### After

```go
func (t Time) After(u Time) bool
```

如果t代表的时间点在u之后，返回真；否则返回假。

```go
package main

import (
	"fmt"
	"time"
)

func main() {
	// 定义两个时间点
	time1 := time.Date(2023, 10, 1, 12, 0, 0, 0, time.UTC)
	time2 := time.Date(2023, 10, 2, 12, 0, 0, 0, time.UTC)

	// 判断时间先后
	fmt.Println(time1.After(time2)) // 输出：false
	fmt.Println(time2.After(time1)) // 输出：true
}
```

## 定时器: time.Tick()

使用`time.Tick(时间间隔)`来设置定时器，定时器的本质上是一个通道（channel）。

返回一个channel，该channel会在每个指定的时间间隔发送当前时间。

```go
func tickDemo() {
    ticker := time.Tick(time.Second) //定义一个1秒间隔的定时器
    for i := range ticker {
        fmt.Println(i)//每秒都会执行的任务
    }
}
```
```go
package main

import (
	"fmt"
	"time"
)

func main() {
	ticker := time.NewTicker(1 * time.Second)
	done := make(chan bool)

	go func() {
		for {
			select {
			case <-done:
				return
			case t := <- ticker.C:
				fmt.Println("Tick at", t)
			}
		}
	}()

	time.Sleep(5 * time.Second)
	ticker.Stop()
	done <- true
	fmt.Println("Ticker stopped")
}
```
```go
Tick at 2024-11-27 12:00:00 +0800 CST
Tick at 2024-11-27 12:00:01 +0800 CST
Tick at 2024-11-27 12:00:02 +0800 CST
Tick at 2024-11-27 12:00:03 +0800 CST
Tick at 2024-11-27 12:00:04 +0800 CST
Ticker stopped
```

## 时间格式化

时间类型有一个自带的方法`time.Format()`进行格式化，需要注意的是Go语言中格式化时间模板不是常见的`Y-m-d H:M:S`而是使用Go的诞生时间2006年1月2号15点04分（记忆口诀为2006 1 2 3 4）。也许这就是技术人员的浪漫吧。

补充：如果想格式化为12小时方式，需指定`PM`。

```go
func formatDemo() {
    now := time.Now()
    fmt.Println(now.Format("2006-01-02 15:04:05"))
    fmt.Println(now.Format("2006/01/02 15:04"))
    fmt.Println(now.Format("15:04 2006/01/02"))
    fmt.Println(now.Format("2006/01/02"))
    // 格式化的模板为Go的出生时间2006年1月2号15点04分 Mon Jan
    // 24小时制
    fmt.Println(now.Format("2006-01-02 15:04:05.000 Mon Jan"))
    // 12小时制
    fmt.Println(now.Format("2006-01-02 03:04:05.000 PM Mon Jan"))
}
```

## 解析时间字符串

```go
now := time.Now()
fmt.Println(now)
// 加载时区
loc, err := time.LoadLocation("Asia/Shanghai")
if err != nil {
    fmt.Println(err)
    return
}
// 按照指定时区和指定格式解析字符串时间
timeObj, err := time.ParseInLocation("2006/01/02 15:04:05", "2019/08/04 14:15:20", loc)
if err != nil {
    fmt.Println(err)
    return
}
fmt.Println(timeObj)
fmt.Println(timeObj.Sub(now))
```
